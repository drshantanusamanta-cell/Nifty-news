nifty-sentiment/
├── app.py                  ← main Streamlit app
├── news_fetcher.py         ← all API + RSS fetch logic
├── sentiment.py            ← FinBERT scoring engine
├── requirements.txt        ← pip dependencies
└── .streamlit/
    └── secrets.toml        ← API keys (local only, never push)
